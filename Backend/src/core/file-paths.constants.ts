import * as path from 'path';
export const uploadFilePath = path.join(__dirname, '..', '..', '..', 'uploads');

export const rejectedFilePath = path.join(
  __dirname,
  '..',
  '..',
  '..',
  'rejected',
);
